management.endpoints.web.exposure.include=health,info

# CRITICAL FIX: Ensure health check details are shown to anonymous users (like Docker's health checker)
management.endpoint.health.show-details=always

# Configure application name (optional)
spring.application.name=Market-Analysis-API
